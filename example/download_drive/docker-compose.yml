version: "3"
# version: "3.6"

services:
  download_drive:
    image: "makevoid/comma_ai_download_drive"
    build:
      context: .
    environment:
      ROUTE_ID: "PASTE_YOUR_ROUTE_ID_HERE"
      JWT_TOKEN: "PASTE_YOUR_JWT_TOKEN_HERE"

# docker-compose up

# docker-compose up --build

# docker run -e ROUTE_ID="xxxxxxxx" makevoid/comma_ai_download_drive
